'use strict';

class Flash {
    constructor(base, count) {
    }

    read(block, buffer, offset = 0) {
    }

    write(block, buffer, offset = 0) {
    }

    ioct(op, ...arg) {
    }
}

module.exports = {
    Flash
};